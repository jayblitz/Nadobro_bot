"""Portfolio v2 — normalized {account → market_segment → {token: {units,
price, value}}} state, history time-series, and distribution. Exposes
state(), history(), distribution(), accounts_distribution().

Implemented in Phase 2.
"""
