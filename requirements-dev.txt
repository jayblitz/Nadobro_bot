# Development-only dependencies. Runtime deps live in requirements.txt.
# Install with:  pip install -r requirements-dev.txt

-r requirements.txt

imagehash>=4.3

# Engine v2 (Phase 1+) static analysis + coverage tooling.
mypy>=1.8
pytest-cov>=5.0
