# SECURITY: fastapi 0.136.3 is malicious (OSV MAL-2026-4750) — that release
# secretly added a typosquat 'fastar' dependency to the [standard] extra,
# giving whoever controls 'fastar' install-time code execution. It is currently
# the latest published version, so an open range resolves straight into it.
# Exclude it explicitly; this resolves to the last good 0.136.1 and will pick up
# a future patched release (0.136.4+) automatically.
fastapi>=0.133.0,!=0.136.3,<1.0
# >=1.3.1 clears CVE-2026-48817/48818 (fix 1.1.0), CVE-2026-54282 (1.3.0),
# CVE-2026-54283 (1.3.1). Kept compatible with the pinned fastapi range.
starlette>=1.3.1,<2.0
uvicorn[standard]>=0.34.0,<1.0
telethon>=1.36.0,<2.0
asyncpg>=0.29.0,<1.0
pydantic>=2.0,<3.0
# Not imported directly here — this is a security floor for Telethon's
# transitive use. >=48.0.1 cleared GHSA-537c-gmf6-5ccf; >=50.0.0 now clears
# CVE-2026-69248/69249 (fixed 49.0.0) and CVE-2026-69247 (fixed 50.0.0).
# Note the <49.0 cap this replaces had pinned the vuln, exactly as the <47.0
# cap did before it — when bumping the floor, move the cap with it.
cryptography>=50.0.0,<51.0
